# Deferred user prompts

Complete these only after the current node-label and Step 2 start-field work is fully reviewed and deployed. Treat each numbered item as a separate prompt. Finish and review one before starting the next.

1. **One mistake per Step 2 case — completed**
   - Across all problems, remove Step 2 cases that ask the student to expose two or more mistakes at once.
   - Every case should expose exactly one mistake.
   - Use parallel adversarial reviewers and iterate until they are happy.

2. **Let students draw before Step 1 questions — completed**
   - Across all problems, find Step 1 questions that show the data and answer choices without giving the student the graph drawing tool first.
   - Make the drawing tool available before the student answers so drawing is encouraged.
   - Use parallel adversarial reviewers and iterate until they are happy.

3. **Remove confusing Step 2 output placeholders — completed**
   - Across all problems, remove placeholder values from Step 2 output fields.
   - In particular, do not show an array placeholder where the real problem returns a number.
   - Use parallel adversarial reviewers and iterate until they are happy.
