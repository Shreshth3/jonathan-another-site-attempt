# Original and New lesson fixes

This ledger covers the 50 Original/New lessons. “Fixed” means the authoring change is present and generated; it does not mean every screen was played manually. Shared runtime/Step2 items are listed separately so their ownership is clear. Root confirmed all 225 starter witnesses, 37 actual-engine regressions, seven semantic fixture validators, and the 75-lesson return validator pass. These are automated checks, not manual completion of all screens.

## Original findings

| Finding | Status and evidence |
| --- | --- |
| O01 | Fixed: bomb node choices distinguish bomb identity from outgoing blast edges. |
| O02 | Shared Step2: bipartition goals must change two-colorability, not only visited nodes; verified by root’s shared checks. |
| O03 | Fixed: duplicate Boolean output choices removed. |
| O04 | Fixed: mechanically corrupted third answers removed; fresh checks execute an actual mistaken algorithm. |
| O05 | Fixed: nested-depth arithmetic corrected; unsupported battleship/phone alternatives replaced. |
| O06 | Fixed: numeric alternatives are computed from a stated mistake; the final fresh-check pass verifies differing results. |
| O07 | Fixed: all Division cases use real traversal; each changes one ratio/identity rule. |
| O08 | Fixed: fresh Step1 inputs and endpoint-specific picture feedback; added Original Step4 proofs now name actual paths, cells, values, or components. Correct diagnoses describe code behavior directly. |
| O09 | Fixed: bomb identity uses indices; Phone has empty guard; Minesweeper uses full reveal; Bipartite uses real cycle DFS; Provinces has separating neighbors; Course, Increasing Paths, and Islands shortcuts replaced with one-bug DFS. |
| O10 | Fixed: correct jug reachability is authored in the repair pass and all generated lesson answers; no reliance on late runtime corrections. |
| O11 | Fixed local oracle/feedback: depth means deepest integer. Source repo's empty-container oracle is deliberately not used for fresh inverse-depth answers. Root added explicit statement wording and the [1,[[]]] example. |
| O12 | Fixed: authored bomb coordinates shifted positive; geometry recomputed. |
| O13 | Shared Step2: course/bipartite witness goals require changed problem answers; verified by root’s shared checks. |
| O14 | Fixed: first-branch diagnosis says the code follows only the first neighbor. |
| O15 | Shared Step2: real bomb geometry fields and validation; verified by root’s shared checks. |
| O16 | Shared Step2: stale ratio metadata removal; verified by root’s shared checks. |
| O17 | Fixed authoring: readable code, direct diagnosis wording, list vocabulary. Shared Step2 handles accepted start labels. |
| O18 | Shared Step2: complete rectangular increasing-grid inputs; verified by root’s shared checks. |
| O19 | Fixed: raw inputs precede neutral questions; prompts no longer partition islands or announce swap connectivity. |
| O20 | Fixed Step1: jug comparisons use valid capacity states; shared Step2 owns its feedback. |
| O21 | Fixed: bomb exact-graph repair contains asymmetric blast directions. |

## New findings

| Finding | Status and evidence |
| --- | --- |
| N01 | Fixed: Boolean explicit-edge case returns buggy true / real false; independent evaluator and manual wrong-then-right Chrome test agree. |
| N02 | Fixed authoring: other trees use fixed index slots; Boolean uses explicit edges and inputIndex:value. Shared Step2 follows the same convention. |
| N03 | Fixed: rejected Boolean diagnosis now makes a specifically false OR claim. |
| N04 | Shared Step2: Flood start-color membership; verified by root’s shared checks. |
| N05 | Shared Step2: Moocast distance/power validation; verified by root’s shared checks. |
| N06 | Shared Step2: Properties threshold and distinct-value overlap; verified by root’s shared checks. |
| N07 | Shared Step2: rectangular farmland validation; verified by root’s shared checks. |
| N08 | Shared Step2: Gold walls/traps and derived draft safety; verified by root’s shared checks. |
| N09 | Shared Step2: labels and scoring values must agree; verified by root’s shared checks. |
| N10 | Shared Step2: scalar items cannot contain children; verified by root’s shared checks. |
| N11 | Fixed authored Properties rows, legal dislike pair order, positive geometry, and valid tree/grid inputs. Shared Step2 owns Restrictions/Fence constraints. |
| N12 | Fixed: fresh Connected Cells correct/mistaken outputs are independently computed on separating inputs. |
| N13 | Fixed: fresh GFG examples expose executable mistakes; repairs are independently checked. |
| N14 | Fixed: generic extra-row/extra-item choices removed; matrix alternatives retain the output structure. |
| N15 | Fixed: two distinct plain Boolean results, without duplicate annotated answers. |
| N16 | Fixed: generic ±1 choices removed; fresh wrong results come from executable misconceptions. |
| N17 | Fixed: all 50 Original/New exact-picture checks have separate fresh inputs. |
| N18 | Fixed: 100 fresh output checks, each with separate repair, solve independently of displayed worked examples. Two checks per lesson use distinct misconceptions. |
| N19 | Fixed: all 150 exact/node/edge checks use fresh drawable inputs; direct-edge repairs require competing rules to give different edge counts or weights. |
| N20 | Fixed authoring: 250 separate Step3 inputs, five per lesson, with at least three topologies and no Step1 repetition. Shared runtime owns balanced claims. |
| N21 | Shared Step2: Boolean witness must change true to false; verified by root’s shared checks. |
| N22 | Fixed: Gold's third case searches normally but drops gold on an entered draft square. |
| N23 | Fixed: Employee overwrite example now has sibling branches with different contributions. |
| N24 | Fixed: Sub-islands says visited rather than erased; Gold explains the outer-wall route around draft stops. |
| N25 | Fixed: node example has empty inner arrays; choices distinguish item/array membership clearly. |
| N26 | Fixed: same-color build now asks about recoloring failing to mark visited, on two adjacent pixels. |
| N27 | Fixed: feedback allows later region compression while grading this lesson's cell graph. |
| N28 | Fixed: Deep Count explanation uses three containment edges. |
| N29 | Fixed: Largest Component minimum initializes from a measured component, not zero. |
| N30 | Fixed: Fish rejected claim falsely says a different pool is skipped, rather than describing ordinary visited behavior. |
| N31 | Fixed: Moocast fresh incorrect counts are produced by the actual wrong algorithm. |
| N32 | Fixed: Tree Sum fresh repairs compute real and mistaken results using the same fixed-slot tree input. |

## Additional Original/New live findings

| Findings | Status and evidence |
| --- | --- |
| L01–L02 | Shared runtime: path answer parsing and saved progress; verified by root’s shared checks. |
| L03–L04 | Fixed: fresh bipartition node input includes differing membership rules; output mistakes are executable and separating. |
| L05 | Fixed: fresh province repairs replace repeated input and include genuinely different components. |
| L06 | Fixed authoring: repeated letters appear at separate indices; shared runtime supplies membership claim wording. |
| L07 | Shared Step2 GFG semantic witness validation; verified by root’s shared checks. |
| L08 | Fixed: picture feedback compares actual missing endpoints/direction and describes the pictured error. |
| L09 | Fixed: edge repairs are selected only when the wrong adjacency rule changes the result. |
| L10 | Fixed: Word fresh checks/repairs use separate boards exposing failed starts and forbidden reuse. |
| L15 | Fixed authored changedGraph on rewritten cases; shared runtime displays changedGraph instead of realGraph. |
| L33–L34 | Fixed: Properties fresh inputs have repeated values and real connected groups; two independently executed misconceptions yield distinct answers. |
| L35 | Fixed: Properties snippets shortened, with readable distinct-value and traversal logic. |
| L36 | Fixed: all three Restrictions Step4 canvases use blocked:true; shared runtime displays required Blue. |
| L37 | Shared runtime: legal restricted reachability; verified by root’s shared checks. |
| L38 | Shared Step2: two allowed branches in the dropped-branch witness; verified by root’s shared checks. |
| L39 | Fixed: fresh Restrictions repairs have allowed descendants behind forbidden ancestors and differ under the wrong algorithm. |

Other L findings belong to variants or shared runtime and are outside this author's scope.

## Verification and regeneration

Final passes, in order:

```sh
node scripts/author-original-new-structure.js
node scripts/author-original-new-fresh-checks.js
node scripts/author-original-new-fresh-rules.js
node scripts/build-visual-data.js
```

For a full reset, run the existing Original/New lesson authors, then `scripts/repair-original-new-audit.js`, then those final passes. Do not run the old global audit patch over the finished Step2/Step4 specifications.

Checks run:

- `validate-step3-coverage.js`: all 75 lessons have five inputs, at least three topologies, and no Step1 repetition.
- `validate-step4-execution.js`: every shown code result agrees with its stored buggy answer.
- `validate-step4-correct-outputs.js`: all 235 cases agree with the reference evaluator, including the independent explicit-edge Boolean parser.
- `validate-authored-graphs.js`: 344 raw-input graphs agree on topology, weights, colors, and blocked nodes.
- `validate-visual-data.js` and `validate-step4-readability.js`: Both pass across the complete 75-lesson library after root’s final Constellations correction.
- `docs/original-new-fresh-checks.json`: 100 correct/wrong output records; generator also checks the 100 fresh repair outputs. Each wrong result differs from the independently computed real result.

Manual Chrome evidence: Boolean Step4 case 3, five nodes and four arrows, entered buggy true and deliberately wrong real true. Only the real result failed; existing work and diagnosis stayed. Correcting real result to false passed, with clear code → changed subtree → OR(false,false) → answer feedback. This is one live case, not a claim that every Original/New screen was manually completed.

Two deliberate limits: the Phone Step1 node cap is 13 because a complete two-digit prefix tree has 1+3+9 nodes; other Step1 assessments stay at nine. The external source repo still has the known inverse-depth empty-list oracle mismatch; the local lesson explicitly uses deepest integer depth instead.
